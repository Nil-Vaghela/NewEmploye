# Core web app
Flask==2.2.3
Werkzeug==2.2.3
Flask-Login==0.6.2
itsdangerous==2.1.2
Jinja2==3.1.2
click==8.1.3

# Data / Excel handling
pandas==1.5.3
numpy==1.24.2
openpyxl==3.1.2

# Used by app.py for US/Eastern timestamps (was missing before - app could not even start without it)
pytz==2024.1

# Misc
schedule==1.2.0
pipreqs==0.4.13
python-dotenv==1.0.1

# Production web server (used on the DigitalOcean deployment, see DEPLOY.md)
gunicorn==21.2.0

# Only needed if you deploy behind uWSGI (see uwsgi.ini). Not required to run
# locally with "python app.py" and can be tricky to build on some machines,
# so it's kept optional here - install separately when you actually deploy:
#   pip install uwsgi
